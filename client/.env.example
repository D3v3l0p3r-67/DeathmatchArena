# Copy to .env.local for local overrides.

# Colyseus endpoint.
#   development -> ws://localhost:2567
#   production  -> wss://<your-app>.colyseus.cloud
# Leave unset to derive it from the page origin, which is correct when the server
# also serves the client build.
VITE_SERVER_URL=ws://localhost:2567

# There is no debug flag here on purpose. Debug tooling (the Shift+D console and
# the F3 overlay) is unlocked by a server-side grant, not by the build -- open the
# game with ?debugToken=<token> and see DEBUG_TOKENS in server/.env.example.
